version: "3.8"
services:

  # Autoscaler Service (Runs the scaling script)
  autoscaler:
    build: ./autoscaler  # Dockerfile for the autoscaler
    depends_on:
      - prometheus
      - app
    networks:
      - prometheus_network 
    restart: always


