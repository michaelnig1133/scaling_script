
services:

  # Autoscaler Service (Runs the scaling script)
  autoscaler:
    build: ./Dockerfile  # Dockerfile for the autoscaler
    # depends_on:
    #   - prometheus
    #   - app
    
    restart: always


